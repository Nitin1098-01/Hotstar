<%= form_with(model: favourite, local: true) do |form| %>
  <% if favourite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favourite.errors.count, "error") %> prohibited this favourite from being saved:</h2>

      <ul>
        <% favourite.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :channel_id %>
    <%= form.select("user_id",options_from_collection_for_select(Channel.all, :id, :name)) %>
   
  </div>

  <div class="field">
  
    <%= form.label :show_id %>
    <%= form.select("show_id", options_from_collection_for_select(Show.all, :id, :name)) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
